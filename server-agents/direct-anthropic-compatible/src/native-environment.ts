import type { AgentNativeEnvironment } from '@garcon/server-agent-interface';

export default {
  integrationId: 'direct-anthropic-compatible',
  directories: [
  ],
} as const satisfies AgentNativeEnvironment;
